<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="UTF-8">
  <title>Fettermann Aikidô - <%= title %>
  </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" 
    rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" 
    crossorigin="anonymous">
    <link rel="stylesheet" href="../../css/consulta-menu.css">
    <link rel="stylesheet" href="../../css/exclusao.css">
    <link rel="icon" href="../../images/logo-sm.png" type="image/png">
  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body>
  <div class="container pt-3 w-100">
      <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
          <a href="/pessoas" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none"> 
              <!--
              <svg class="bi me-2" width="40" height="32" aria-hidden="true"><use xlink:href="#bootstrap"></use></svg> 
              -->
              <img src="../../images/logo-sm.png"/>
              <span class="fs-4">Fettermann Aikidô</span>
          </a> 

          <%- include('partials/menu_principal') %>
      </header>
  </div>
  <main>
    <div class="container pt-3 w-100">
      <div class="mb-2">
        <a class="btn btn-primary btn-sm" href="/cobrancas/edita/<%= doc._id %>">Edita</a>
        <a class="btn btn-primary btn-sm" href="/cobrancas">Cancela</a>
      </div>

      <div class="card">
        <div class="card-header">Dados da cobrança (Consulta)</div>
        <div class="card-body">
          
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Pessoa: </span>
                <span id="pessoa" name="pessoa"><%= doc.pessoa.nome %></span>
            </div>
          </div>

          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Taxa: </span>
                <span id="taxa" name="taxa"><%= doc.taxa.descricao %></span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Descrição: </span>
                <span id="valor" name="valor"><%= doc.descricao %></span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Valor devido: R$</span>
                <span id="valor" name="valor"><%= doc.valor %></span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Data de emissão (dd/mm/aaaa):</span>
                <span id="data_emissao" name="data_emissao"><%= doc.data_emissao %></span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Data de vencimento (dd/mm/aaaa):</span>
                <span id="data_vencimento" name="data_vencimento"><%= doc.data_vencimento %></span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Situação: </span>
                <span id="situacao" name="situacao"><%= doc.situacao.toUpperCase() %></span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Período: </span>
                <span id="periodo" name="periodo"><%= doc.periodo_referencia %></span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Observacoes: </span>
                <span id="observacoes" name="observacoes"><%= doc.observacoes %></span>
            </div>
          </div>

          <div class="card mb-3 mt-2">
            <div class="card-header fw-bold">Pagamentos</div>
            <div class="card-body">    
              <div class="col-12">
              <% if (mensagem) { %>
                  <div id="mensagem-topo" class="alert alert-info alert-dismissible" >
                      <span id="texto-mensagem"><%= mensagem %></span>
                      <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                  </div>
              <% } %>



              <ol id="lista" name="lista" class="list-group list-group-flush">
                <% if (doc.pagamentos) { %>
                  <% doc.pagamentos.forEach(function(pagamento) { %>
                    <li class="list-group-item" data-id="<%= pagamento._id %>">
                      <a href="/cobrancas/pagamento/edita/<%= pagamento._id %>">
                      Pagamento de R$ <%= pagamento.valor_pago %> efetuado em <%= pagamento.data_pagamento %>
                      através de <%= pagamento.forma_pagamento %>.
                      </a>
                      <% if (pagamento.desconto>0) { %>
                        <p>Desconto aplicados: <%= pagamento.desconto %></p>
                      <% } %>
                      <% if (pagamento.juros>0) { %>
                        <p>Juros aplicados: <%= pagamento.juros %></p>
                      <% } %>

                      <div class="actions">
                        Ações: 
                        <a class="delete-link" 
                          data-action="show-confirm" 
                          data-cobranca-id="<%= doc._id %>" 
                          data-id="<%= pagamento._id %>">Excluir</a>
                        
                        <div class="confirm-section" id="confirm-<%= pagamento._id %>">
                          <span class="confirm-message">Confirmar exclusão?</span>
                          <button class="btn btn-yes" 
                                  data-action="confirm-delete-pagamento" 
                                  data-cobranca-id="<%= doc._id %>" 
                                  data-id="<%= pagamento._id %>">Sim</button>
                          <button class="btn btn-no" 
                                  data-action="hide-confirm" 
                                  data-cobranca-id="<%= doc._id %>" 
                                  data-id="<%= pagamento._id %>">Não</button>
                        </div>
                      </div>
                    </li>
                  <% }); %>
                <% } else {%>
                  <p>Nenhum pagamento registrado.</p>
                <% } %>
              </ol>
              </div>
            </div>
          </div>
          
        </div>
      </div>
    </div>
  </main>

  <div class="container"> 
    <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top"> 
        <div class="col-md-4 d-flex align-items-center"> 
          <span class="mb-3 mb-md-0 text-body-secondary">2025 - Fettermann Aikidô</span>
        </div> 
        <ul class="nav col-md-4 justify-content-end list-unstyled d-flex"></ul> 
    </footer> 
  </div>
    
  <!-- JavaScript Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" 
  integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" 
  crossorigin="anonymous"></script>
  <script src="/js/exclusao.js"></script>
</body>

</html>