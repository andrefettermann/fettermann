<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="UTF-8">
  <title>Fettermann Aikidô - <%= title %>
  </title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.3/dist/css/bootstrap.min.css" rel="stylesheet"
      integrity="sha384-rbsA2VBKQhggwzxH7pPCaAqO46MgnOM80zW1RWuH61DGLwZJEdK2Kadq2F9CUG65" crossorigin="anonymous">

  <meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body>
  <header>
  </header>
  <main>
    <div class="container pt-3 w-75">
            
      <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
        <a href="/pessoas" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none"> 
          <img src="/images/logo-sm.png"/>
          <span class="fs-4">Fettermann Aikidô</span>
        </a> 

        <ul class="nav nav-pills"> 
          <li class="nav-item"><a href="/dojos" class="nav-link">Dojos</a></li> 
          <li class="nav-item"><a href="/pessoas" class="nav-link active" aria-current="page">Pessoas</a></li> 
          <li class="nav-item"><a href="/graduacoes" class="nav-link">Graduações</a></li> 
          <li class="nav-item"><a href="/logout" class="nav-link">Sair</a></li> 
        </ul>
      </header>

      <div class="card">
        <div class="card-header">Dados da pessoa</div>
        <div class="card-body">
          <div class="row pt-1">
            <div class="col-10"></div>
            <div class="col-2">
              <a class="btn btn-primary" href="/pessoas/edita/<%= doc._id%>">Edita</a>
              <a class="btn btn-primary" href="/pessoas">Cancela</a>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Nome: </span>
                <span id="nome" name="nome"><%= doc.nome %></span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Dojo: </span>
                <span id="dojo" name="dojo">
                  <% if (doc.dojo[0]) { %>
                    <%= doc.dojo[0].nome %>
                  <% } %>
                </span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Matrícula: </span>
                <span id="matricula" name="matricula"><%= doc.matricula %></span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">CPF: </span>
                <span id="cpf" name="cpf"><%= doc.cpf %></span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Aniversário: </span>
                <span id="aniversario" name="aniversario"><%= doc.aniversario %></span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Graduação atual: </span>
                <span id="graduacao_atual" name="graduacao_atual"><%= doc.graduacao_atual %></span>
            </div>
          </div>
          <div class="row pt-2">
            <div class="col">
                <span class="fw-bold">Situação: </span>
                <span id="situacao" name="situacao"><%= doc.situacao %></span>
            </div>
          </div>

          <div class="card mb-3">
            <div class="card-header fw-bold">Promoções</div>
            <div class="card-body">
              <ol id="promocoes" name="promocoes" class="list-group list-group-flush">
                <% if (doc.promocoes) { %>
                      <% doc.promocoes.forEach(function(promocao){ %>
                        <li class="list-group-item"><%= promocao.data_formatada + " - " + promocao.graduacao %></li>
                      <% }); %>
                <% }; %>
              </ol>
            </div>
          </div>

          <div class="card mb-3">
            <div class="card-header fw-bold">Pagamentos</div>
            <div class="card-body">    
              <ol id="pagamentos" name="pagamentos" class="list-group list-group-flush">
                <% if (doc.pagamentos) { %>
                      <% doc.pagamentos.forEach(function(pagamento){ %>
                        <li class="list-group-item"><%= pagamento.data_formatada 
                          + " - " + pagamento.valor_pago + " - " + pagamento.descricao 
                          + " - " + pagamento.observacoes %></li>
                      <% }); %>
                <% }; %>
              </ol>
            </div>
          </div>

        </div>
      </div>

      <div class="container"> 
          <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top"> 
              <div class="col-md-4 d-flex align-items-center"> 
                  <span class="mb-3 mb-md-0 text-body-secondary">v. 2025.01 Fettermann Aikidô</span>
                </div> 
                <ul class="nav col-md-4 justify-content-end list-unstyled d-flex"></ul> 
          </footer> 
      </div>

    </div>
  </main>
</body>

</html>