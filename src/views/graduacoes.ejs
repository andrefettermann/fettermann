<!DOCTYPE html>
<html lang="pt-br">

<head>
    <meta charset="UTF-8">
    <title>Fettermann Aikidô - <%= title %>
    </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" 
    rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" 
    crossorigin="anonymous">
    <link rel="stylesheet" href="../css/consulta-menu.css">
    <link rel="stylesheet" href="../css/exclusao.css">
    <link rel="icon" href="../images/logo-sm.png" type="image/png">
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body>
    <div class="container pt-3 w-100">
        <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
            <a href="/pessoas" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none"> 
                <!--
                <svg class="bi me-2" width="40" height="32" aria-hidden="true"><use xlink:href="#bootstrap"></use></svg> 
                -->
                <img src="../images/logo-sm.png"/>
                <span class="fs-4">Fettermann Aikidô</span>
            </a> 

            <%- include('partials/menu_principal') %>
        </header>
    </div>

    <main>

        <div class="container pt-3 w-100">
            <div class="mb-2">
                <a id="botao_nova_graduacao" name="botao_nova_graduacao" 
                class="btn btn-success btn-sm" href="/graduacoes/novo">Nova graduação</a>
            </div>

            <input class="form-control mt-2 mb-2" id="myInput" type="text" placeholder="Filtrar...">

            <div class="card">
                <div class="card-header fw-bold">
                    <%= total %> graduação(ões) encontrada(s)
                </div>
                <div class="card-body">
                    <div class="row">
                        <div class="col-12">
                            <% if (mensagem) { %>
                                <div id="mensagem-topo" class="alert alert-info alert-dismissible" >
                                    <span id="texto-mensagem"><%= mensagem %></span>
                                    <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
                                </div>
                            <% } %>

                                <ul id="lista" class="list-group mb-2">
                            <% docs.forEach(function(doc){ %>
                                    <li class="list-group-item" data-id="<%= doc.id %>">
                                        <h5>
                                            <%= doc.nome %>
                                            (Faixa <%= doc.faixa %> - <%= doc.categoria %>)
                                        </h5>
                                        <hr>
                                        <%= doc.minimo_horas_treino_exame %> horas de treino na faixa atual para exame<br/>
                                        <%= doc.minimo_tempo_exame %> meses de treino na faixa atual para exame<br/>
                                        <br/>
                                        
                                        <div class="actions">
                                            Ações: 
                                        <a href="/graduacoes/detalhes/<%= doc.id %>" class="px-2"> Detalhes </a>
                                          |  
                                        <a href="/graduacoes/edita/<%= doc.id %>" class="px-2"> Editar </a>
                                         | 
                                         <a class="delete-link" data-action="show-confirm" data-id="<%= doc._id %>">Excluir</a>
                                         <div class="confirm-section" id="confirm-<%= doc.id %>">
                                           <span class="confirm-message">Confirmar exclusão?</span>
                                           <button class="btn btn-yes" data-action="confirm-delete-graduacao" data-id="<%= doc.id %>">Sim</button>
                                           <button class="btn btn-no" data-action="hide-confirm" data-id="<%= doc.id %>">Não</button>
                                         </div>
                                        </div>
                                    </li> 
                            <% }); %>
                                </ul>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </main>

    <div class="container"> 
        <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top"> 
            <div class="col-md-4 d-flex align-items-center"> 
                <span class="mb-3 mb-md-0 text-body-secondary">2025 - Fettermann Aikidô</span>
                </div> 
                <ul class="nav col-md-4 justify-content-end list-unstyled d-flex"></ul> 
        </footer> 
    </div>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
    <!-- JavaScript Bundle with Popper -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" 
    integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" 
    crossorigin="anonymous"></script>
    <script src="/js/filtra_lista.js"></script>
    <script src="/js/exclusao.js"></script>
</body>

</html>