<!DOCTYPE html>
<html lang="pt-br">

<head>
  <meta charset="UTF-8">
  <title>Fettermann Aikidô - <%= title %>
  </title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/css/bootstrap.min.css" 
    rel="stylesheet" integrity="sha384-sRIl4kxILFvY47J16cr9ZwB07vP4J8+LH7qKQnuqkuIAvNWLzeN8tE5YBujZqJLB" 
    crossorigin="anonymous">
    <link rel="stylesheet" href="../../css/consulta-menu.css">
    <link rel="icon" href="../../images/logo-sm.png" type="image/png">
    <meta name="viewport" content="width=device-width, initial-scale=1">
</head>

<body>
  <div class="container pt-3 w-100">
      <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
          <a href="/pessoas" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto link-body-emphasis text-decoration-none"> 
              <!--
              <svg class="bi me-2" width="40" height="32" aria-hidden="true"><use xlink:href="#bootstrap"></use></svg> 
              -->
              <img src="../../images/logo-sm.png"/>
              <span class="fs-4">Fettermann Aikidô</span>
          </a> 

          <%- include('partials/menu_principal') %>
      </header>
  </div>
  <main>
    <div class="container pt-3 w-75">
      <div class="card">
        <div class="card-header"><%= title %></div>
        <div class="card-body">
          <form id="formulario" name="formulario" action="<%= action %>" 
            method="POST">

            <!-- Info Alert -->
            <div class="alert alert-info alert-dismissible fade show">
              <strong>Info!</strong> O '*'' indica os campos obrigatórios.
              <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>

            <div id="mensagem-topo" class="alert alert-danger alert-dismissible d-none" >
              <span id="texto-mensagem"></span>
              <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
            </div>

            <!-- Tipo -->
            <div class="form-group row mb-3">
              <div class="col-2">
                <label for="tipo" class="col-form-label">*Tipo</label>
              </div>
              <div class="col-2">
                <select id="tipo" name="tipo" class="form-select" aria-label="Tipo"
                data-obrigatorio="Por favor, selecione o tipo.">
                  <option value="" selected>Selecione</option>
                  <option <%= doc.tipo=='anuidade'?'selected':'' %> value="anuidade">Anuidade</option>
                  <option <%= doc.tipo=='aula_avulsa'?'selected':'' %> value="aula_avulsa">Aula avulsa</option>
                  <option <%= doc.tipo=='exame'?'selected':'' %> value="exame">Exame</option>
                  <option <%= doc.tipo=='inscricao_evento'?'selected':'' %> value="exame"> value="inscricao_evento">Inscrição em evento</option>
                  <option <%= doc.tipo=='mensalidade'?'selected':'' %> value="mensalidade">Mensalidade</option>
                  <option <%= doc.tipo=='promocao'?'selected':'' %> value="promocao">Promoção</option>
                </select>
              </div>
            </div>

            <!-- Nome -->
            <div class="form-group row mb-3">
              <div class="col-2">
                <label for="nome" class="col-form-label">*Nome</label>
              </div>
              <div class="col-3">
                <input type="text" class="form-control" id="nome" name="nome" 
                value="<%= doc.nome %>"
                placeholder="O nome da taxa" title="O nome da taxa" 
                data-obrigatorio="Por favor, preencha o nome.">
              </div>
            </div>

            <!-- Descricao -->
            <div class="form-group row mb-3">
              <div class="col-2">
                <label for="descricao" class="col-form-label">Descrição</label>
              </div>
              <div class="col-6">
                <input type="text" class="form-control" id="descricao" 
                name="descricao" value="<%= doc.descricao %>"
                placeholder="Descrição da taxa" aria-colcount="10" 
                data-toggle="tooltip" data-placement="top" 
                title="Descrição da taxa">
              </div>
            </div>

            <!-- Ativa? -->
            <div class="form-group row mb-3">
              <div class="col-2">
                <label for="is_ativa" class="form-label">Ativa?</label>
              </div>
              <div class="col-2">
                <input class="form-check-input" type="checkbox" id="is_ativa" name="is_ativa" <%= doc.is_ativa?'checked':''%>>
              </div>
            </div>

            <!-- Recorrente? -->
            <div class="form-group row mb-3">
              <div class="col-2">
                <label for="is_recorrente" class="form-label">Recorrente?</label>
              </div>
              <div class="col-2">
                <input class="form-check-input" type="checkbox" id="is_recorrente" name="is_recorrente" <%= doc.is_recorrente?'checked':''%>>
              </div>
            </div>

            <!-- Periodicidade -->
            <div class="form-group row mb-3">
              <div class="col-2">
                <label for="periodicidade" class="col-form-label">Periodicidade</label>
              </div>
              <div class="col-2">
                <select id="periodicidade" name="periodicidade" class="form-select" aria-label="Tipo">
                  <option value="" selected>Selecione</option>
                  <option <%= doc.periodicidade=='anual'?'selected':''%> value="anual">Anual</option>
                  <option <%= doc.periodicidade=='mensal'?'selected':''%> value="mensal">Mensal</option>
                  <option <%= doc.periodicidade=='unica'?'selected':''%> value="unica">Única</option>
                </select>
              </div>
            </div>

            <!-- Valor padrao -->
            <div class="form-group row mb-3">
              <div class="col-2">
                <label for="valor_padrao" class="col-form-label">Valor padrão (R$)</label>
              </div>
              <div class="col-2">
                <input type="text" class="form-control" id="valor_padrao" name="valor_padrao" 
                value="<%= doc.valor_padrao.$numberDecimal %>"
                placeholder="O valor bruto da taxa" 
                aria-colcount="10" data-toggle="tooltip" data-placement="top" 
                title="O valor bruto da taxa">
              </div>
            </div>

            <div class="col-12">
              <input id="botao_grava" name="botao_grava" class="btn btn-primary" type="submit" value="Grava" />
              <a id="botao_cancela" name="botao_cancela" class="btn btn-primary" href="/taxas">Cancela</a>
            </div>

          </form>
        </div>
      </div>
    </div>
  </main>

  <div class="container"> 
      <footer class="d-flex flex-wrap justify-content-between align-items-center py-3 my-4 border-top"> 
          <div class="col-md-4 d-flex align-items-center"> 
              <span class="mb-3 mb-md-0 text-body-secondary">v. 2025.02 - Taxas - Fettermann Aikidô</span>
            </div> 
            <ul class="nav col-md-4 justify-content-end list-unstyled d-flex"></ul> 
      </footer> 
  </div>

  <script src="/js/verifica_campos_obrigatorios.js"></script>
  <!-- JavaScript Bundle with Popper -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.8/dist/js/bootstrap.bundle.min.js" 
  integrity="sha384-FKyoEForCGlyvwx9Hj09JcYn3nv7wiPVlz7YYwJrWVcXK/BmnVDxM+D2scQbITxI" 
  crossorigin="anonymous"></script>
</body>

</html>